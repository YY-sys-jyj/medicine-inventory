# 医药库存动销与回款管理系统 - 运维说明

## 自动提醒

- 推送时间：北京时间 07:50、16:50。
- 触发位置：`.github/workflows/daily-reminders.yml`
- 推送接口：`supabase/functions/wxpusher/index.ts`
- 认证方式：GitHub Secret `CRON_SECRET` 必须与 Supabase Edge Function Secret `CRON_SECRET` 一致。
- 推送对象：系统按 `user_id` 分开生成提醒，只推给该用户绑定的 PushPlus/WxPusher。

## 客户绑定 WxPusher

客户不需要进入你的 Supabase、GitHub 或 WxPusher 管理后台。客户只需要：

1. 打开或下载 WxPusher。
2. 找到自己的 `UID_xxxxx`。
3. 登录本系统，进入“通知中心 -> 微信提醒绑定”。
4. 把自己的 UID 填入“客户自己的 WxPusher UID”，保存绑定。
5. 点“发送测试”，能收到测试消息就说明绑定成功。

之后系统会按客户自己的账号数据自动推送。不同客户的数据和 UID 按 `user_id` 分开，不会互相串消息。

## 数据保留

定时推送执行时会顺手清理过期运营数据：

- 通知记录：保留 90 天。
- PushPlus 临时扫码绑定会话：保留 30 天。
- 出入库日志、回款日志、删除日志：保留 365 天。

业务台账数据不会被自动清理。

## 本地检查

```bash
npm test
```

该命令会检查 `docs/index.html`、`docs/index-secure.html`、`docs/supabase.js` 的脚本语法。

## Edge Function 部署

```bash
npx supabase functions deploy wxpusher --project-ref lrvsblbnyismfmithqse
```

## 常见验证

- GitHub Pages 页面：https://yy-sys-jyj.github.io/medicine-inventory/
- Supabase Functions：https://supabase.com/dashboard/project/lrvsblbnyismfmithqse/functions
- GitHub Actions：https://github.com/YY-sys-jyj/medicine-inventory/actions

如果自动推送失败，优先看 GitHub Actions 的 `Daily reminder push` 日志；如果手动测试失败，再看 Supabase Edge Function Secrets 是否缺少 `WXPUSHER_APP_TOKEN`、`PUSHPLUS_TOKEN`、`PUSHPLUS_SECRET_KEY` 或 `CRON_SECRET`。
